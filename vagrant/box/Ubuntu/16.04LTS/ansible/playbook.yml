---
- hosts: 127.0.0.1
  connection: local
  sudo: yes
  tasks:
    - name: install via apt-get
      apt: pkg={{ item }} update_cache=yes
      with_items:
        - nginx
        - tomcat7
        - mysql-server
